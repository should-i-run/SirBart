/* @flow */
import {
  StyleSheet,
} from 'react-native';

import {genericText} from '../styles';

export default StyleSheet.create({
  item: {
    ...genericText,
  },
  picker: {
    width: 350,
    height: 200,
  },
});
